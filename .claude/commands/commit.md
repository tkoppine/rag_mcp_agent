Run `git status` and `git diff` to understand what changed. Run `git log --oneline -5` to match the commit style. Stage all modified files including `.claude/` files (be specific, avoid secrets/temp files like .env). Write a 1-2 sentence commit message focused on WHY the change was made. Commit using a HEREDOC with this trailer: `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`. Run `git status` after to confirm success. Report which files were committed and the final commit message.
